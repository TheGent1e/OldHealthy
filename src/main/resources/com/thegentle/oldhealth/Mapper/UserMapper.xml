<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thegentle.oldhealth.Mapper.UserMapper">

<!--    //注册用户-->
    <insert id="register">
        insert into users(username,password,name,gender,age,phone,email,health_record_number,role,status,created_at,updated_at)
        values(#{username},#{password},#{name},#{gender},#{age},#{phone},#{email},#{healthRecordNumber},#{role},#{status},#{created_at},#{updated_at})
    </insert>
    <!--    //获取用户基本信息-->
    <insert id="addUser">
        insert into users(username,password,name,gender,age,phone,email,health_record_number,role,status)
        values(#{username},#{password},#{name},#{gender},#{age},#{phone},#{email},#{healthRecordNumber},#{role},#{status})
    </insert>
    <!--    //修改用户-->
    <update id="updateUser">
        update users set username=#{username},password=#{password},name=#{name},gender=#{gender},age=#{age},phone=#{phone},email=#{email},health_record_number=#{healthRecordNumber},role=#{role},status=#{status} where id=#{id}
    </update>
<!--    //删除用户-->
    <delete id="deleteUser">
        delete from users where id=#{id}
    </delete>
    <select id="getUserList" resultType="com.thegentle.oldhealth.pojo.User.User">
        select id,username,name,role,gender,age,phone,email,health_record_number,status from users
        <where>
            <if test="username!=null and username!=''">
                username like '%${username}%'
            </if>
            <if test="phone!=null and phone!=''">
                and phone like '%${phone}%'
            </if>
            <if test="email!=null and email!=''">
                and email like '%${email}%'
            </if>
            <if test="role!=null and role!=''">
                and role=#{role}
            </if>
            <if test="status!=null and status!=''">
                and status=#{status}
            </if>
            <if test="begin!=null and begin!=''">
                and create_time&gt;=#{begin}
            </if>
            <if test="end!=null and end!=''">
                and create_time&lt;=#{end}
            </if>
        </where>
    </select>
</mapper>